# Claude Code 작업 지침 — WiNNoW (레포: winnow-claude)

이 파일은 Claude Code가 이 프로젝트에서 작업할 때 지킬 지침이다.
프로젝트의 전체 맥락·계획은 `README.md`(마스터 플랜)를 참고한다.

## 진행 방식
- 한 번에 다 만들지 말 것. README의 "6. 남은 일 > v1 구현"에 있는 단계를 **하나씩** 구현한다.
- 한 단계를 끝내면 멈추고, 어떻게 테스트하는지 알려준다. 내가 확인한 뒤 다음 단계를 지시한다.
- 큰 구조를 바꾸거나 새 라이브러리를 추가할 땐 먼저 이유를 설명하고 물어본다.

## git
- git 작업이 필요하면, **터미널에 칠 정확한 명령어만 알려준다.** 나 대신 실행하지 말 것.
- 나는 터미널·git을 익히는 중이라, 직접 쳐보며 배우려 한다.

## 설명 방식
- 한국어로. 쉬운 문장, 구체적 예시, 짧게 끊어서.
- 내 코딩 수준은 학부 수준의 파이썬(+ C#). 어려운 개념은 풀어서 설명한다.

## 이 프로젝트에서 절대 지킬 것
- **원본 사진을 삭제하거나 옮기지 않는다. 복사만 한다.** (오판으로 사진을 잃으면 안 됨)
- 조절 설정값(keep 비율·유사도 경계·흐림 경계)은 코드 맨 위에 모아, 쉽게 바꿀 수 있게 둔다.
- 무료·오프라인 원칙. 유료 API나 인터넷 연결이 필요한 방식은 쓰지 않는다.

## 완료 기준
- "완벽"이 아니라 "내가 쓰기에 충분함"으로 판단한다. (README의 "7. 완료 기준" 참고)
