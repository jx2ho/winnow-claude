# WiNNoW가 쓰는 라이브러리 (전부 무료·오프라인)
Pillow          # 사진 열기·EXIF(촬영 시각) 읽기
pillow-heif     # 아이폰 HEIC 사진도 열 수 있게
exifread        # RAW(CR2·NEF·ARW 등)에서 촬영 시각 읽기
rawpy           # RAW를 그림으로 열기 (내장 미리보기 꺼내기 / 없으면 현상)
numpy           # 유사도 지문(dHash) 계산
